# D3 QR is fully client-side and does not currently require any environment variables.
# This file exists as a placeholder for future configuration if needed.
#
# Note: any value placed here would be exposed in the bundled JavaScript and visible
# to anyone who views the page source. Never put secrets here.
